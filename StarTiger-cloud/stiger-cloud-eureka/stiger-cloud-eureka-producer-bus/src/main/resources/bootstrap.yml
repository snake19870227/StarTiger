spring:
  cloud:
    config:
#      uri: http://localhost:9000
      name: config,config2
      profile: dev
      discovery:
        enabled: true
        service-id: config-server-git
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20000/eureka/